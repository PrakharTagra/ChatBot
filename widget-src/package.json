{
  "name": "chatbot-widget-builder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src.js --bundle --minify --outfile=../widget/chat-widget.js --platform=browser --target=es2017 --legal-comments=none",
    "build:dev": "esbuild src.js --bundle --outfile=../widget/chat-widget.js --platform=browser --target=es2017 --sourcemap"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}