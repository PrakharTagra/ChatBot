{
  "name": "chatbot-root",
  "version": "1.0.0",
  "description": "Root package — delegates to backend/",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
